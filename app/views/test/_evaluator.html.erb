<ul>
  <% @evaluation.each_pair do |k, v| %>
    <% if v  %>
    <% degree = Degree.find(k) %>
    <li>
      <h3><%= degree.name %></h3>
      <p>
        <%= degree.description %>
      </p>
      <h4>Puntuacion: <%= v %></h4>
    </li>
    <% end %>
  <% end %>
</ul>
